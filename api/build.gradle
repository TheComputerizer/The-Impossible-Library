dependencies {
    implementation("com.google.code.findbugs:jsr305:${project.jsr_version}")
    implementation("com.moandjiezana.toml:toml4j:${project.toml4j_version}")
    implementation("io.netty:netty-all:${project.netty_version}")
    implementation("org.apache.commons:commons-lang3:${project.lang3_version}")
    implementation("org.apache.logging.log4j:log4j-core:${project.log4j_version}")
    implementation("org.joml:joml:${project.joml_version}")
}

subprojects {
    dependencies {
        implementation("com.moandjiezana.toml:toml4j:${project.toml4j_version}") {  //for toml files
            exclude group: 'com.google.code.gson', module: 'gson'
        }
    }
}