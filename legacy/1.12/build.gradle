//file:noinspection DependencyNotationArgument
//file:noinspection GroovyAssignabilityCheck

/**
 * 1.12.2 forge build
 */

plugins {
    alias libs.plugins.shadow
    alias libs.plugins.unimined
}

extendFrom project, 'shadow'

uniminedCore project, 'forge', true, [ dev_arg, common_entrypoint_arg, core_entrypoint_arg ]

setRepositories 'cf', 'ct', 'hei'

dependencies {
    api project(':api')
    api project(':legacy')

    annotationProcessor libs.bundles.legacy.annotations

    compileOnly modsBundle(project, 'legacy', 'compile')

    implementation modsBundle(project, 'legacy', 'implementation')

    runtimeOnly modsBundle(project, 'legacy', 'runtime')
}

inheritCompileSources ':api', ':legacy'

inheritResources ':legacy'

setJarsRunAfter true, ':legacy'

remapJarDefaults project

setSharedShadowJar ':api', ':legacy'

tasks.assemble.dependsOn shadowJar