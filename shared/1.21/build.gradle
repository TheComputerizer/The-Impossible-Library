//file:noinspection GroovyAssignabilityCheck

/**
 * Shared implementations for 1.21
 */

configurations {
    dev {
        canBeResolved = false
        canBeConsumed = true
    }
}

artifacts {
    dev jar
}