//file:noinspection GroovyAssignabilityCheck

/**
 * Shared implementations for 1.18
 */
plugins {
    alias libs.plugins.shadow
    alias libs.plugins.unimined
}

extendFrom project, 'shadow'

uniminedCommon project
uniminedFabric project, true
uniminedForge project, true

setSharedDependencies false, false, 'cf', 'ct', 'jei', 'modrinth', 'modmenu'

inheritResources ':shared'

setJarsRunAfter true, ':shared'