repositories {
    mavenCentral() {
        content {
            includeGroup lombok_group
        }
    }
}

dependencies {
    def lombok_full = "$lombok_group:$lombok_name:$lombok_version".toString()
    annotationProcessor lombok_full
    compileOnly lombok_full
}