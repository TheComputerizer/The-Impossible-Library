/**
 * Experimental changes/additions that either need further testing or might have better alternatives
 */

if(parent!=null && 'api'!=name) {
    evaluationDependsOn ':api'
    if(name.contains('shared')) {
        evaluationDependsOn ':fabric'
        evaluationDependsOn ':forge'
        //noinspection GrUnresolvedAccess
        if(isNeoforgeCompatible()) evaluationDependsOn ':neoforge'
    }
}